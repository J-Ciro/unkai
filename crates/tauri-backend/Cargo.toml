[package]
name = "tauri-backend"
version = "0.1.0"
edition = "2021"
[dependencies]
tauri = { version = "2" }
serde = { version = "1.0", features = ["derive"] }
anyhow = "1"
engine-core = { path = "../engine-core" }
platform-windows = { path = "../platform-windows" }
